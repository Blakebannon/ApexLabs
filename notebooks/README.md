# Notebooks

Notebooks are for exploration and communication, not the sole record of an analysis. A result eligible for a finding must move essential preprocessing and analysis into versioned, tested package code and record datasets, hashes, versions, configuration, and randomness. Clear outputs that expose private telemetry before committing.
