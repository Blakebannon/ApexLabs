# Research records

This directory holds reviewable protocols' outputs: machine-readable findings, metric definitions, and later algorithm specifications. It does not hold raw telemetry or notebook-only state.

Every finding remains immutable by version. Corrections create a new version and preserve the old artifact. Findings are organized by current status; moving status requires a new reviewed finding version, not rewriting history without explanation.
