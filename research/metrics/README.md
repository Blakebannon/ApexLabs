# Metric definitions

Metric records define meaning, unit, directionality, required normalized inputs, computation, output provenance, and limitations. A metric name alone is never an adequate specification. Changing meaning or computation requires a new version.
