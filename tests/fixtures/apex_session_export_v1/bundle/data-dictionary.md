# Synthetic data dictionary

`telemetry.csv` contains one-metre bins. Brake is the maximum. Throttle,
steering radians, and speed metres per second are arithmetic means. Empty
channels are unavailable and are never measured zero.
